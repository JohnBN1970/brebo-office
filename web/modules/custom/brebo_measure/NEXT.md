# Next engineering increment

Before the first mobile proof, add deterministic capture lifecycle transitions and validation writes:

`draft -> captured -> validating -> accepted|rejected`

Then expose an Office action that creates/selects an opening and starts a measurement assignment. The mobile proof can use the v0.1 API contract while the UI is still minimal.
