# Canonical capture contract

A mobile client never owns the canonical opening. It contributes versioned evidence to an Office-owned opening through an assignment and capture.

Every submitted measurement remains attributable to its source/method and may be compared with other observations for the same semantic key. This contract is the bridge from today's mobile proof to later precision hardware and FrameBot.
