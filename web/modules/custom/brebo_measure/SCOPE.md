# Measure-02 scope freeze

In scope: thin Office JSON transport for existing MeasureRepository operations and the field-validation contract needed by the first mobile proof.

Out of scope: production release, ordering, automatic geometry reconstruction, AI validation, native iOS UI, external customer authentication and FrameBot control.

Keeping this boundary small lets the first real-opening experiment start before larger product work is committed.
