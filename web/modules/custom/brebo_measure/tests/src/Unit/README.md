# Measure API tests

The first tests intentionally guard the domain and field-workflow contract while the controller remains a thin transport layer. Integration tests against a bootstrapped Drupal database follow with validation/status transitions.
