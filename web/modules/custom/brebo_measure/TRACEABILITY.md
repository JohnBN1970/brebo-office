# Traceability chain

Every practical result should remain traceable through:

`building object -> opening -> assignment -> capture/version -> observation -> method/device -> validation`

Later production geometry may reference accepted measurement evidence, but it must not destroy or rewrite that evidence. This is what allows BREBO to investigate a future fit problem back to the original measurement source.
