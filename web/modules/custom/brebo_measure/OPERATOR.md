# Operator-first principle

The field workflow must be faster than the paperwork it replaces. The target interaction is:

`select opening -> measure -> confirm requested references -> control dimension -> finish`

If the operator needs to understand the underlying data model, the capture UX is not finished. Complex reasoning belongs in Office after capture, not in the hands of the person standing at the opening.
