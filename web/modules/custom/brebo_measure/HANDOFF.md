# Handoff to Measure-03

After Measure-02 merges, implement repository-backed lifecycle transitions and validation writes, then a minimal Office action to start a measurement session. Keep native sensor work separate so backend validation can evolve independently.
