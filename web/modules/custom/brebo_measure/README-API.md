# Office API overview

Measure-02 exposes the smallest useful path from BREBO Office to a future mobile capture client. The API follows the existing `/brebo-office/api/...` convention and keeps responses private/no-store.

The API is intentionally not a production-ordering API. Its job is to create an auditable measurement dossier that Office can later validate and turn into an approved production decision.
