# Measure-02 — Office workflow and API

Goal: make the Measure domain callable by the first mobile capture client without embedding Apple-specific assumptions in BREBO Office.

Acceptance path:

`opening -> assignment -> capture -> observations -> later validation`

The next increment should add capture status transitions and validation persistence to the repository/API, then wire the first Office screen to create/select a sparing and launch a mobile measurement session.
