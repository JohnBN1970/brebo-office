# CI review note

Open the PR only after the branch diff is confirmed to be based directly on the #604 merge. Let the repository PHP quality gate decide coding-standard compatibility; patch the branch rather than bypassing the gate if it fails.
