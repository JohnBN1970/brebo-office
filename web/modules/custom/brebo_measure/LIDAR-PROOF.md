# First LiDAR proof outcome

The proof succeeds if Office receives repeatable sensor observations and independent control measurements for a real opening with enough traceability to explain deviations.

It does not require every opening feature to be mobile-only. Discovering that a feature needs a simple reference or precision accessory is a successful engineering result because it tells us exactly what hardware is worth building.
