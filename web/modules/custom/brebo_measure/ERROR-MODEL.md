# Measurement error model v0.1

BREBO Measure must distinguish at least:

- accuracy: difference from an independent reference;
- repeatability: spread across repeated captures;
- uncertainty: expected range reported/estimated for an observation;
- completeness: whether required geometry was captured;
- operator correction: manual intervention needed after sensor capture.

The first field tests collect evidence for all five rather than reducing quality to a single accuracy number.
