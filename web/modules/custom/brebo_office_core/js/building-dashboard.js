(function (Drupal, once) {
  'use strict';
  const TILE_SIZE = 256;
  function project(lat, lon, zoom) { const scale = TILE_SIZE * Math.pow(2, zoom); const latitude = Math.max(-85.05112878, Math.min(85.05112878, lat)); const sin = Math.sin(latitude * Math.PI / 180); return {x: ((lon + 180) / 360) * scale, y: (0.5 - Math.log((1 + sin) / (1 - sin)) / (4 * Math.PI)) * scale}; }
  function chooseZoom(markers) { if (markers.length < 2) return 13; const lats = markers.map((m) => Number(m.lat)); const lons = markers.map((m) => Number(m.lon)); const span = Math.max(Math.max(...lats) - Math.min(...lats), Math.max(...lons) - Math.min(...lons)); if (span > 4) return 6; if (span > 2) return 7; if (span > 1) return 8; if (span > .5) return 9; if (span > .2) return 10; if (span > .08) return 11; if (span > .03) return 12; return 13; }
  function escapeHtml(value) { const div = document.createElement('div'); div.textContent = String(value == null ? '' : value); return div.innerHTML; }
  function renderMap(element, allMarkers) {
    let markers = allMarkers; let zoom = chooseZoom(markers); let center = centerOf(markers);
    const canvas = document.createElement('div'); canvas.className = 'brebo-buildings-map__canvas'; element.replaceChildren(canvas);
    const tiles = document.createElement('div'); tiles.className = 'brebo-buildings-map__tiles'; canvas.appendChild(tiles);
    const markersLayer = document.createElement('div'); markersLayer.className = 'brebo-buildings-map__markers'; canvas.appendChild(markersLayer);
    const controls = document.createElement('div'); controls.className = 'brebo-buildings-map__controls'; controls.innerHTML = '<button type="button" data-map-zoom-in aria-label="Inzoomen">+</button><button type="button" data-map-zoom-out aria-label="Uitzoomen">−</button>'; canvas.appendChild(controls);
    const attribution = document.createElement('a'); attribution.className = 'brebo-buildings-map__attribution'; attribution.href = 'https://www.openstreetmap.org/copyright'; attribution.target = '_blank'; attribution.rel = 'noopener noreferrer'; attribution.textContent = '© OpenStreetMap'; canvas.appendChild(attribution);
    function centerOf(items) { return items.length ? {lat: items.reduce((s,m) => s + Number(m.lat),0) / items.length, lon: items.reduce((s,m) => s + Number(m.lon),0) / items.length} : {lat: 52.15, lon: 5.3}; }
    function draw() {
      tiles.replaceChildren(); markersLayer.replaceChildren(); const width = element.clientWidth || 900; const height = element.clientHeight || 440; const centerPoint = project(center.lat, center.lon, zoom); const left = centerPoint.x - width / 2; const top = centerPoint.y - height / 2; const maxTile = Math.pow(2, zoom);
      for (let x = Math.floor(left/TILE_SIZE); x <= Math.floor((left+width)/TILE_SIZE); x++) for (let y = Math.floor(top/TILE_SIZE); y <= Math.floor((top+height)/TILE_SIZE); y++) { if (y < 0 || y >= maxTile) continue; const image = document.createElement('img'); image.className='brebo-buildings-map__tile'; image.alt=''; image.loading='lazy'; image.src=`https://tile.openstreetmap.org/${zoom}/${((x%maxTile)+maxTile)%maxTile}/${y}.png`; image.style.left=`${x*TILE_SIZE-left}px`; image.style.top=`${y*TILE_SIZE-top}px`; tiles.appendChild(image); }
      markers.forEach((marker) => { const point=project(Number(marker.lat),Number(marker.lon),zoom); const button=document.createElement('button'); button.type='button'; button.className='brebo-buildings-map__marker'; button.style.left=`${point.x-left}px`; button.style.top=`${point.y-top}px`; button.setAttribute('aria-label',marker.title||'Gebouw'); button.innerHTML='<span></span>'; button.addEventListener('click',()=>{ markersLayer.querySelectorAll('.brebo-buildings-map__popup').forEach((p)=>p.remove()); const popup=document.createElement('div'); popup.className='brebo-buildings-map__popup'; popup.style.left=`${point.x-left}px`; popup.style.top=`${point.y-top}px`; popup.innerHTML=`<strong>${escapeHtml(marker.title)}</strong><span>${escapeHtml(marker.address)}${marker.city ? ', '+escapeHtml(marker.city):''}</span><span>${Number(marker.units)||0} BAG-eenheden</span><a href="${escapeHtml(marker.url)}">Gebouw openen</a>`; markersLayer.appendChild(popup); }); markersLayer.appendChild(button); });
    }
    element.breboFilterMarkers = (query) => { const needle=query.trim().toLocaleLowerCase('nl'); markers = needle ? allMarkers.filter((m)=>(m.search||`${m.title} ${m.address} ${m.city}`).toLocaleLowerCase('nl').includes(needle)) : allMarkers; if (markers.length) { center=centerOf(markers); zoom=chooseZoom(markers); } draw(); };
    controls.querySelector('[data-map-zoom-in]').addEventListener('click',()=>{zoom=Math.min(18,zoom+1);draw();}); controls.querySelector('[data-map-zoom-out]').addEventListener('click',()=>{zoom=Math.max(5,zoom-1);draw();}); draw(); let timer; window.addEventListener('resize',()=>{window.clearTimeout(timer);timer=window.setTimeout(draw,120);});
  }
  Drupal.behaviors.breboBuildingsDashboardMap = { attach(context) { once('brebo-buildings-dashboard-map','[data-brebo-buildings-map]',context).forEach((element)=>{ let markers=[]; try {markers=JSON.parse(element.dataset.markers||'[]');} catch(e){markers=[];} markers=markers.filter((m)=>Number.isFinite(Number(m.lat))&&Number.isFinite(Number(m.lon))); if(markers.length) renderMap(element,markers); }); } };
  Drupal.behaviors.breboBuildingsDashboardSearch = { attach(context) { once('brebo-buildings-dashboard-search','[data-brebo-building-search]',context).forEach((input)=>{ const dashboard=input.closest('.brebo-buildings-dashboard'); if(!dashboard)return; const rows=[...dashboard.querySelectorAll('[data-brebo-building-table] tbody tr')]; const count=dashboard.querySelector('[data-brebo-building-search-count]'); const noResults=dashboard.querySelector('[data-brebo-building-no-results]'); const map=dashboard.querySelector('[data-brebo-buildings-map]'); const filter=()=>{ const needle=input.value.trim().toLocaleLowerCase('nl'); let visible=0; rows.forEach((row)=>{ const match=!needle||(row.dataset.search||row.textContent).toLocaleLowerCase('nl').includes(needle); row.hidden=!match; if(match)visible++; }); if(count)count.textContent=`${visible} ${visible===1?'gebouw':'gebouwen'}`; if(noResults)noResults.hidden=visible!==0; if(map&&typeof map.breboFilterMarkers==='function')map.breboFilterMarkers(needle); }; input.addEventListener('input',filter); }); } };
})(Drupal, once);
