services:
  brebo_sandbox_reset.manager:
    class: Drupal\brebo_sandbox_reset\Service\SandboxResetManager
    arguments: ['@database', '@entity_type.manager', '@entity_field.manager', '@state', '@queue']
