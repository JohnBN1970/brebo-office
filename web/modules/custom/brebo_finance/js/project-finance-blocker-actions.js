(function(Drupal,once){'use strict';
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]));
const dateValue=ts=>ts?new Date(Number(ts)*1000).toISOString().slice(0,10):'';
Drupal.behaviors.breboFinanceBlockerActions={attach(context){once('bfp-blocker-actions','#brebo-finance-project-detail',context).forEach(async app=>{const root=app.querySelector('[data-bfpd-content]');if(!root||!app.dataset.ledgerUrl)return;const projectMatch=location.pathname.match(/\/finance\/projects\/(\d+)/),projectNid=Number(projectMatch?.[1]||0);if(!projectNid)return;const listUrl=`/brebo-office/api/finance/projects/${projectNid}/invoice-blocker-actions`,saveUrl='/brebo-office/api/finance/invoice-blocker-actions',csrf=()=>fetch('/session/token',{credentials:'same-origin'}).then(r=>r.text());
let actions=[];try{const r=await fetch(listUrl,{credentials:'same-origin'});if(r.ok)actions=await r.json();}catch(e){console.warn('BREBO blocker actions:',e.message);}const byLine=()=>new Map((actions||[]).filter(a=>!['resolved','cancelled'].includes(a.status)).map(a=>[String(a.invoice_line_id),a]));
const apply=()=>{const map=byLine();root.querySelectorAll('.bfpd-blocker-card').forEach(card=>{if(card.dataset.actionEnhanced==='1')return;const m=card.querySelector('h3')?.textContent.match(/#(\d+)/);if(!m)return;const lineId=Number(m[1]),existing=map.get(String(lineId));card.dataset.actionEnhanced='1';const box=document.createElement('section');box.className='bfpd-blocker-action';box.innerHTML=`<div class="bfpd-blocker-action-head"><strong>Opvolging</strong><span>${existing?esc(existing.status):'Nog niet toegewezen'}</span></div><form><label>Verantwoordelijke UID<input name="owner_uid" type="number" min="1" required value="${esc(existing?.owner_uid||'')}"></label><label>Deadline<input name="due_date" type="date" value="${dateValue(existing?.due_date)}"></label><label>Actie<textarea name="action" required placeholder="Wat moet er gebeuren?">${esc(existing?.action||'')}</textarea></label><label>Status<select name="status">${['open','in_progress','waiting','resolved','cancelled'].map(s=>`<option value="${s}"${existing?.status===s?' selected':''}>${s}</option>`).join('')}</select></label><button type="submit">${existing?'Opvolging bijwerken':'Actie toewijzen'}</button><small data-action-state></small></form>`;card.appendChild(box);const form=box.querySelector('form'),state=box.querySelector('[data-action-state]');form.onsubmit=async e=>{e.preventDefault();const fd=new FormData(form),owner=Number(fd.get('owner_uid')),action=String(fd.get('action')||'').trim(),due=String(fd.get('due_date')||''),status=String(fd.get('status')||'open');if(!owner||!action)return;const button=form.querySelector('button');button.disabled=true;state.textContent='Opslaan…';try{const token=await csrf(),r=await fetch(saveUrl,{method:'POST',credentials:'same-origin',headers:{'Content-Type':'application/json','X-CSRF-Token':token},body:JSON.stringify({invoice_line_id:lineId,project_nid:projectNid,owner_uid:owner,action,due_date:due?Math.floor(new Date(`${due}T12:00:00`).getTime()/1000):null,status})});const data=await r.json().catch(()=>({}));if(!r.ok)throw new Error(data.message||'Opvolging kon niet worden opgeslagen.');actions=(actions||[]).filter(a=>Number(a.id)!==Number(data.id)&&Number(a.invoice_line_id)!==lineId);actions.push(data);state.textContent='✓ Opgeslagen';box.querySelector('.bfpd-blocker-action-head span').textContent=data.status;}catch(err){state.textContent=err.message;}finally{button.disabled=false;}};});};apply();new MutationObserver(apply).observe(root,{childList:true,subtree:true});});}};})(Drupal,once);
